---
layout: layouts/base
---
<section class="mb-20">
  <div class="text-lg md:text-xl mb-20">
    <p class="">
      Design and development of tailored websites and digital systems that are useful, simple, and effortless to maintain. Currently <a href="#contact" class="text-green-500">available</a> for new commissions, consulting, and development services for other designers and agencies.
    </p>
  </div>
  <ul class="flex flex-col" x-data="{
    showAll: true, 
  }"
  >
    {%- for item in sites -%}
      {% if item.disabled == true or item.url == '' %}
        <li 
        class="flex sm:grid grid-cols-8 gap-y-1 justify-between relative py-3 border-b border-gray-400 -mt-px"
        {% if not item.featured %}
        x-show="showAll"
        {% endif %}
        >
          <div class="col-span-2">
            <h2 class="text-gray-400">{{ item.name }}</h2>
          </div>
          <div class="col-span-5 hidden sm:block">
            <p class="text-gray-400">{{ item.text }}</p>
          </div>
          <div class="col-span-1 text-right">
            {{ item.year }}
          </div>
        </li>
      {% else %}
        <a class="link | group" href="{{ item.url }}" target="_blank" noopener noreferrer data-frame-info-top="" data-frame-info-btm="
              <div class='flex flex-wrap gap-4'>
                <span class='name text-sm'>{{ item.name }}</span>
                {% for tech in item.tech %}
                  <div class='flex gap-2 items-center'>
                    <div class='w-3 h-3 rounded-full relative overflow-hidden flex text-sm'>
                      <i class='w-full h-full' data-tech='{{ tech }}'></i>
                    </div>
                    <span class='techname opacity-50 text-sm'>{{ tech }}</span>
                  </div>
                {% endfor %}
                </div>
                ">
          <li 
          class="flex sm:grid grid-cols-8 gap-y-1 justify-between relative py-3 border-b border-gray-400 -mt-px"
          {% if not item.featured %}
          x-show="showAll"
          {% endif %}
          >
            <div class="col-span-2 mr-2">
              <h2 class="">{{ item.name }}</h2>
            </div>
            <div class="col-span-5 hidden sm:block">
              <p class="text-gray-400">{{ item.text }}</p>
            </div>
            <div class="col-span-1 text-right">
            {{ item.year }}
          </div>
          </li>
        </a>
    {% endif %}
    {% endfor %}
  </ul>
</section>

<section class="py-32 md:py-48" id="contact">
  <div class="w-full text-center flex items-center justify-center">
    <a class="fitty" href="mailto:{{ site.email }}">{{site.email}}</a>
  </div>
</section>